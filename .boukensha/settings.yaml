tasks:
  player:
    provider: opencode        # provider name (string)
    model: deepseek-v4-flash-free
    prompt_override:
      system: true
mud:
  host: localhost
  port: 4000
  username: dummy
  password: helloworld

reset:
  admin:
    username: dummy
    password: helloworld
  start_room: "3001"
  # Expected room NAME for start_room. The reset verifies the player actually
  # lands here and retries on mismatch (live vnums can differ from the offline DB).
  start_room_name: "The Temple Of Midgaard"
  max_attempts: 3