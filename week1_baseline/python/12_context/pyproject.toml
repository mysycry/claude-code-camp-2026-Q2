[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "boukensha"
version = "0.1.0"
description = "Boukensha agent configuration system"
requires-python = ">=3.14"
dependencies = [
    "python-dotenv>=1.1.0",
    "pyyaml>=6.0",
]

[tool.ruff]
line-length = 120
