#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"

export BOUKENSHA_DIR="$REPO_ROOT/.boukensha"

cd "$SCRIPT_DIR/../../ruby/09_global_executable"
ruby bin/boukensha